CORRECAO BUILD FLUTTER - AuthService

Este patch adiciona os metodos usados pelo register_screen.dart:
- finalizeCompanyRegistrationFace(...)
- cancelPendingCompanyRegistration(...)

Extraia este ZIP na raiz do projeto SteelControl Mobile e substitua os arquivos.
Depois rode:
  flutter clean
  flutter pub get
  flutter analyze
  flutter run
